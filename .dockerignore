# VCS & editor junk

.git
.gitignore
.svn
.DS_Store
.vscode
.idea

# Builds / caches

**/node_modules
**/dist
**/.turbo
**/.next
**/coverage
**/\*.log

# Local env & secrets (keep prod env files if you need them)

.env
\*.env.local
!.env.production

# SQLite files in repo (Docker uses a volume for DB)

packages/infra/db/_.sqlite
packages/infra/db/_.db

.git
node_modules
apps/**/node_modules
packages/**/node_modules
dist
build
Dockerfile
docker-compose.yml
