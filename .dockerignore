# VCS & editor junk
.git
.gitignore
.svn
.DS_Store
.vscode
.idea

# Builds / caches
**/node_modules
**/dist
**/.turbo
**/.next
**/coverage
**/*.log

# Local env & secrets (keep prod env files if you need them)
.env
*.env.local
!.env.production

# SQLite files in repo (Docker uses a volume for DB)
packages/infra/db/*.sqlite
packages/infra/db/*.db
