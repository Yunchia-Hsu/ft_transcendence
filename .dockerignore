# VCS & editor junk
.git
.svn
.DS_Store
.vscode
.idea

# Installs / caches
**/node_modules
**/.turbo
**/.next
**/coverage
**/*.log

# Builds (мы копируем их из builder-стейджа, поэтому в контекст не нужны)
# добавь реинклюды:
!apps/api/dist/**
!packages/infra/dist/**
build

# Local env & secrets
# По умолчанию — не тащим, НО оставляем api/.env, т.к. оно нужно в образ
.env
*.env.local
!.env.production
!apps/api/.env
!apps/api/.env.local

# SQLite в репозитории не нужны (в контейнере том)
packages/infra/db/*.sqlite
packages/infra/db/*.db

# Не игнорировать эти файлы (оставьте их доступны Docker/Compose)
# Dockerfile
# docker-compose.yml
